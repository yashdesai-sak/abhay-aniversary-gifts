use love meter and rewrite the code
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <title>Our 1 Year ❤️</title>

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">

  <style>

    *{
      margin:0;
      padding:0;
      box-sizing:border-box;
      font-family:'Poppins',sans-serif;
      scroll-behavior:smooth;
    }

    body{
      background:#0f0f0f;
      color:white;
      overflow-x:hidden;
    }

    /* ===== Floating Hearts ===== */

    .heart{
      position:fixed;
      width:20px;
      height:20px;
      background:#ff4d6d;
      transform:rotate(45deg);
      animation:float 10s linear infinite;
      opacity:0.5;
      z-index:-1;
    }

    .heart::before,
    .heart::after{
      content:'';
      width:20px;
      height:20px;
      background:#ff4d6d;
      border-radius:50%;
      position:absolute;
    }

    .heart::before{
      top:-10px;
      left:0;
    }

    .heart::after{
      left:-10px;
      top:0;
    }

    @keyframes float{
      0%{
        transform:translateY(100vh) rotate(45deg);
        opacity:0;
      }

      100%{
        transform:translateY(-120vh) rotate(45deg);
        opacity:0.7;
      }
    }

    /* ===== HERO ===== */

    .hero{
      height:100vh;
      display:flex;
      justify-content:center;
      align-items:center;
      flex-direction:column;
      text-align:center;
      padding:20px;

      background:
      linear-gradient(rgba(0,0,0,0.6),rgba(0,0,0,0.7)),
      url('https://images.unsplash.com/photo-1518199266791-5375a83190b7?q=80&w=1600') center/cover;
    }

    .hero h1{
      font-size:4rem;
      margin-bottom:20px;
      animation:fadeDown 1.5s ease;
    }

    .hero p{
      max-width:700px;
      font-size:1.2rem;
      line-height:1.8;
      animation:fadeUp 2s ease;
    }

    .btn{
      margin-top:30px;
      padding:15px 35px;
      border:none;
      border-radius:50px;
      background:#ff4d6d;
      color:white;
      cursor:pointer;
      transition:0.4s;
      font-size:1rem;
      text-decoration:none;
    }

    .btn:hover{
      transform:scale(1.08);
      box-shadow:0 0 25px #ff4d6d;
    }

    @keyframes fadeDown{
      from{
        opacity:0;
        transform:translateY(-50px);
      }

      to{
        opacity:1;
        transform:translateY(0);
      }
    }

    @keyframes fadeUp{
      from{
        opacity:0;
        transform:translateY(50px);
      }

      to{
        opacity:1;
        transform:translateY(0);
      }
    }

    /* ===== SECTION ===== */

    section{
      padding:100px 20px;
    }

    h2{
      text-align:center;
      font-size:3rem;
      margin-bottom:50px;
    }

    /* ===== TIMELINE ===== */

    .timeline-container{
      max-width:900px;
      margin:auto;
      display:flex;
      flex-direction:column;
      gap:40px;
    }

    .memory{
      background:#1a1a1a;
      padding:25px;
      border-radius:25px;
      transition:0.4s;
      box-shadow:0 0 20px rgba(255,77,109,0.15);
    }

    .memory:hover{
      transform:translateY(-10px);
    }

    .memory img{
      width:100%;
      border-radius:20px;
      margin-top:15px;
    }

    /* ===== LOVE NOTES ===== */

    .notes{
      text-align:center;
    }

    .note-box{
      max-width:700px;
      margin:auto;
      background:#1a1a1a;
      padding:40px;
      border-radius:25px;
      min-height:150px;
      display:flex;
      justify-content:center;
      align-items:center;
      font-size:1.3rem;
      box-shadow:0 0 20px rgba(255,77,109,0.2);
    }

    /* ===== LOVE METER ===== */

    .meter-container{
      text-align:center;
    }

    .meter{
      width:280px;
      height:280px;
      border-radius:50%;
      margin:40px auto;
      display:flex;
      justify-content:center;
      align-items:center;
      font-size:3rem;
      font-weight:bold;
      background:conic-gradient(
      #ff4d6d 0deg,
      #ff4d6d 0deg,
      #222 0deg
      );

      box-shadow:0 0 40px rgba(255,77,109,0.4);
      transition:1s;
    }

    .meter-inner{
      width:220px;
      height:220px;
      background:#111;
      border-radius:50%;
      display:flex;
      justify-content:center;
      align-items:center;
      flex-direction:column;
    }

    /* ===== FINAL ===== */

    .final{
      text-align:center;

      background:
      linear-gradient(rgba(0,0,0,0.7),rgba(0,0,0,0.8)),
      url('https://images.unsplash.com/photo-1516589178581-6cd7833ae3b2?q=80&w=1600') center/cover;
    }

    .final p{
      max-width:700px;
      margin:auto;
      line-height:1.8;
      font-size:1.2rem;
    }

    /* ===== MUSIC ===== */

    .music-btn{
      position:fixed;
      top:20px;
      right:20px;
      width:55px;
      height:55px;
      border:none;
      border-radius:50%;
      background:#ff4d6d;
      color:white;
      font-size:22px;
      cursor:pointer;
      z-index:999;
    }

    /* ===== MOBILE ===== */

    @media(max-width:768px){

      .hero h1{
        font-size:2.5rem;
      }

      h2{
        font-size:2.2rem;
      }

      .meter{
        width:220px;
        height:220px;
      }

      .meter-inner{
        width:170px;
        height:170px;
      }

    }

  </style>
</head>

<body>

  <!-- MUSIC -->

  <audio id="music" loop>
    <source src="your-music.mp3" type="audio/mp3">
  </audio>

  <button class="music-btn" onclick="toggleMusic()">🎵</button>

  <!-- FLOATING HEARTS -->

  <script>

    for(let i=0;i<30;i++){

      let heart=document.createElement('div');

      heart.classList.add('heart');

      heart.style.left=Math.random()*100+'vw';

      heart.style.animationDuration=
      (Math.random()*5+5)+'s';

      heart.style.width=
      heart.style.height=
      (Math.random()*20+10)+'px';

      document.body.appendChild(heart);
    }

  </script>

  <!-- HERO -->

  <section class="hero">

    <h1>Happy 1 Year ❤️</h1>

    <p>
      One whole year of memories, laughter,
      chaos, late-night talks, and loving you.
      And honestly... I’d choose you every single time.
    </p>

    <a href="#timeline" class="btn">
      Start Our Story ✨
    </a>

  </section>

  <!-- TIMELINE -->

  <section id="timeline">

    <h2>Our Journey 💖</h2>

    <div class="timeline-container">

      <div class="memory">
        <h3>🌸 First Meet</h3>

        <p>
          The moment my life secretly changed forever.
        </p>

        <img src="photo1.jpg">
      </div>

      <div class="memory">
        <h3>📸 First Date</h3>

        <p>
          Still one of my favorite memories ever.
        </p>

        <img src="photo2.jpg">
      </div>

      <div class="memory">
        <h3>✨ Endless Memories</h3>

        <p>
          Every photo with you feels special.
        </p>

        <img src="photo3.jpg">
      </div>

    </div>

  </section>

  <!-- LOVE NOTES -->

  <section class="notes">

    <h2>Reasons I Love You ❤️</h2>

    <div class="note-box" id="note">
      Click below for a surprise 💌
    </div>

    <button class="btn" onclick="showLove()">
      Reveal Love ✨
    </button>

  </section>

  <!-- LOVE METER -->

  <section class="meter-container">

    <h2>Love Meter 💘</h2>

    <div class="meter" id="meter">

      <div class="meter-inner">

        <span id="percent">0%</span>

        <small>Compatibility</small>

      </div>

    </div>

    <button class="btn" onclick="startLoveMeter()">
      Check Our Love ❤️
    </button>

  </section>

  <!-- FINAL -->

  <section class="final">

    <h2>Forever With You 🌙</h2>

    <p>
      This year has been the most beautiful part
      of my life. Thank you for every smile,
      every memory, and every moment.
      I love you endlessly ❤️
    </p>

  </section>

  <!-- SCRIPT -->

  <script>

    /* ===== LOVE NOTES ===== */

    const messages = [

      "You make every day brighter ☀️",

      "You are my favorite person ❤️",

      "I still get butterflies 🦋",

      "Life feels complete with you ✨",

      "You are my safe place 🌙",

      "Every memory with you is precious 💖",

      "You're the best thing that happened to me 💌"

    ];

    function showLove(){

      const random =
      messages[Math.floor(Math.random()*messages.length)];

      document.getElementById('note').innerHTML = random;

    }

    /* ===== LOVE METER ===== */

    function startLoveMeter(){

      let meter = document.getElementById("meter");

      let percentText =
      document.getElementById("percent");

      let value = 0;

      let finalValue =
      Math.floor(Math.random()*10)+91;

      let interval = setInterval(()=>{

        value++;

        percentText.innerHTML = value + "%";

        meter.style.background =
        `conic-gradient(
        #ff4d6d ${value*3.6}deg,
        #222 ${value*3.6}deg
        )`;

        if(value >= finalValue){

          clearInterval(interval);

        }

      },25);

    }

    /* ===== MUSIC ===== */

    const music =
    document.getElementById("music");

    let playing = false;

    function toggleMusic(){

      if(!playing){

        music.play();

        playing = true;

      }

      else{

        music.pause();

        playing = false;

      }

    }

  </script>

</body>
</html>
